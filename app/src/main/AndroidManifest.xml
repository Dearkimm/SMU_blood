<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.smu.blood">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SMU_blood">
        <!--Download-->
        <activity
            android:name=".ui.download.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.download.loginActivity"
            android:exported="true"/>
        <activity
            android:name=".ui.download.SignUpActivity"
            android:exported="true"/>

        <activity
            android:name=".NavigationActivity"
            android:exported="true"/>

        <!--Board-->
        <activity
            android:name=".ui.board.BoardWritingActivity"
            android:exported="true"/>


        <!-- Google Map API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB57Q2RnPpZ7n7oLc5Ky9K7wBUPTsa395o" />

        <activity
            android:name=".ui.map.MapApplicationActivity"
            android:exported="true" />
    </application>

</manifest>